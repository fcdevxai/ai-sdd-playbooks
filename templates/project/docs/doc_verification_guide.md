# Verification guide

> The exact project commands `sdd-apply`, `sdd-code-review`, and `sdd-verify`
> run. Keep these accurate — the lifecycle depends on them.

## Commands

| Purpose | Command |
|---|---|
| Format | `<project format command>` |
| Lint / type-check | `<command>` |
| Feature/domain tests | `<command>` |
| Full regression | `<command>` |

## Notes

- <how to run a single test, coverage expectations, etc.>
